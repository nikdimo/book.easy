# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.next-build/
/.next-previous/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local agent/runtime state (may contain request URLs or personal data)
/.codex/

# local deployment control panel (contains VPS connection metadata)
/BookEasy_Web_Control_Panel.bat

# env files
.env
.env.local
.env.production

# local dev secrets / test accounts (do not publish)
test-users.txt
/.secrets/

# uploads
/public/uploads

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

# local tooling scratch (not part of the app)
/.codex-backups/
/.tmp.driveupload/
/.tmp.drivedownload/
/Listing-Explorer/
/.claude/

# Local Replit UI export workspaces (large generated copies, including node_modules)
/replit/
/replit-ui-export-*/

# Local screen recordings and their transcripts (design reference, hundreds of MB;
# GitHub rejects blobs over 100 MB). Keep these on disk, out of git.
*.mp3
*.wmv
*.mp4
/New admin screen recording transcript.docx
