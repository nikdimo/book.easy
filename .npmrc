# Temporary compatibility bridge: Auth.js beta.32 has not widened its optional
# Nodemailer peer range to v9 yet. The application uses the compatible public APIs,
# while v9.0.4 contains the security fix missing from supported v8 releases.
legacy-peer-deps=true
